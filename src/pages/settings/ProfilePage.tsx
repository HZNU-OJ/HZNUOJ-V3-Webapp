import React from "react";

import SettingsLayout from "@/layouts/SettingsLayout";

const ProfilePage: React.FC<{}> = (props) => {
  return <SettingsLayout current="profile"></SettingsLayout>;
};

export default ProfilePage;
